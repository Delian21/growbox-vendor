import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ══════════════════════════════════════════════
  // ── PRIMARY — Emerald Green ──
  // ══════════════════════════════════════════════
  static const Color primary = Color(0xFF1A7F50);
  static const Color primaryLight = Color(0xFF22A064);
  static const Color primaryDark = Color(0xFF14603A);
  static const Color primarySurface = Color(0xFFE8F5EE);

  // ── Accent — Terracotta Orange ──
  static const Color accent = Color(0xFFE07B39);
  static const Color accentLight = Color(0xFFFEF0E8);
  static const Color accentDark = Color(0xFFC4662A);

  // ── Brand Orange (from logo) ──
  static const Color brandOrange = Color(0xFFE07B39);
  static const Color brandOrangeLight = Color(0xFFFEF0E8);
  static const Color brandOrangeDark = Color(0xFFC4662A);

  // ── Error — True Red ──
  static const Color error = Color(0xFFDC2626);
  static const Color errorLight = Color(0xFFFEE2E2);

  // ── Success — Fresh Green ──
  static const Color success = Color(0xFF16A34A);
  static const Color successLight = Color(0xFFDCFCE7);

  // ── Warning — Bright Amber ──
  static const Color warning = Color(0xFFF59E0B);
  static const Color warningLight = Color(0xFFFEF3C7);

  // ── Backgrounds & Surfaces ──
  static const Color background = Color(0xFFF9F8F4);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF3F2EE);
  static const Color secondarySurface = Color(0xFFFFFFFF);
  static const Color sidebarSurface = Color(0xFFF0EFEA);
  static const Color border = Color(0xFFE5E5E0);
  static const Color borderLight = Color(0xFFEDEDEA);
  static const Color secondaryBorder = Color(0xFFD4D4CF);

  // ── Text ──
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF5C5C5C);
  static const Color textTertiary = Color(0xFF8C8C8C);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ── Info — Clean Blue ──
  static const Color info = Color(0xFF2563EB);
  static const Color infoLight = Color(0xFFEFF6FF);

  // ── Order Status ──
  static const Color pending = Color(0xFFF59E0B);
  static const Color pendingLight = Color(0xFFFEF3C7);
  static const Color accepted = Color(0xFF2563EB);
  static const Color acceptedLight = Color(0xFFEFF6FF);
  static const Color preparing = Color(0xFF8B5CF6);
  static const Color preparingLight = Color(0xFFF3F0FF);
  static const Color ready = Color(0xFF0891B2);
  static const Color readyLight = Color(0xFFECFEFF);
  static const Color completed = Color(0xFF16A34A);
  static const Color completedLight = Color(0xFFDCFCE7);
  static const Color cancelled = Color(0xFFDC2626);
  static const Color cancelledLight = Color(0xFFFEE2E2);

  // ── Chart Colors ──
  static const Color chartGreen = Color(0xFF1A7F50);
  static const Color chartGreenLight = Color(0xFF6DD5A0);
  static const Color chartBlue = Color(0xFF2563EB);
  static const Color chartOrange = Color(0xFFE07B39);

  // ── Glassmorphism ──
  static const Color glassLight = Color(0xB3FDFCFA);
  static const Color glassDark = Color(0xB3121612);
  static const Color glassBorderLight = Color(0x22000000);
  static const Color glassBorderDark = Color(0x1AFFFFFF);

  // ── Background Gradient (light mode) ──
  static const Color bgGradientStart = Color(0xFFF9F8F4);
  static const Color bgGradientEnd = Color(0xFFF5F4F0);

  // ══════════════════════════════════════════════
  // ── DARK THEME — Deep Forest ──
  // ══════════════════════════════════════════════
  static const Color darkBackground = Color(0xFF0D0F0D);
  static const Color darkSurface = Color(0xFF1A1E1A);
  static const Color darkSurfaceVariant = Color(0xFF222822);
  static const Color darkBorder = Color(0xFF333A33);
  static const Color darkTextPrimary = Color(0xFFFFFFFF);
  static const Color darkTextSecondary = Color(0xFFA8B0A5);
  static const Color darkTextTertiary = Color(0xFF6B7569);

  // ── Dark Accent — Emerald ──
  static const Color darkPrimary = Color(0xFF34D399);
  static const Color darkPrimarySurface = Color(0xFF132E1F);

  // ── Dark Accent — Terracotta ──
  static const Color darkAccent = Color(0xFFF09060);
  static const Color darkAccentSurface = Color(0xFF2A1A12);

  // ── Dark Card Sheen ──
  static const Color darkCardSheen = Color(0xFF1E2E1A);

  // ── Dark Elevation Shadows ──
  static const Color darkShadow = Color(0x200A120A);
  static const Color darkCardShadow = Color(0x280A120A);
}